/*Const function addnumbers(products{
	let totalsum = 0
	products.forEach(product =>{totalSum += product.price})
})
return totalsum*/

const CartSubtotal = () => {
	return <input type="text" placeholder="" />;
};

export default CartSubtotal;
